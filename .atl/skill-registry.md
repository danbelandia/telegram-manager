# Skill Registry — TelegramManager

> Generated by sdd-init (hybrid mode). Scan date: 2026-09-05.
> Registry path: `.atl/skill-registry.md`. User skills dir: `~/.config/opencode/skills/`.

## Project Standards (conventions)

### AGENTS.md (project index)
Path: `AGENTS.md` — fuente de verdad del MVP: alcance (Fase 1 only), arquitectura monolito modular, seguridad (JWT access 15min + refresh 7d cookie httpOnly, bcrypt cost 12), rate limiter token bucket ~25 req/s en TelegramService, migraciones goose, tests con TelegramService mockeado (moq), Telegram como fuente de verdad (no inventar endpoints de la Bot API). No Redis/microservicios. `admins` incluye password_hash/created_at/last_login_at.

Referenced files from index (include in every context):
- `docs/telegram_api_reference.md` — referencia de la Bot API; consultar antes de implementar llamadas a Telegram.
- `docs/backend-go-skill.md` — guía de estilo/arquitectura backend Go.
- `docs/frontend-react-skill.md` — guía de estilo frontend React/TypeScript.

## User Skills

### branch-pr
- Path: `~/.config/opencode/skills/branch-pr/SKILL.md`
- Trigger: crear/abrir/preparar PRs.
- Rules: PR MUST link issue con label `status:approved`; MUST tener exactamente un label `type:*`; ramas `type/description` (regex `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)\/[a-z0-9._-]+$`); commit convencional `type(scope): desc`; sin `Co-Authored-By`; checks automáticos deben pasar antes del merge.

### chained-pr
- Path: `~/.config/opencode/skills/chained-pr/SKILL.md`
- Trigger: PRs >400 líneas o SDD forecast `High`/`Chained PRs recommended`.
- Rules: dividir PRs >400 changed lines salvo `size:exception` aprobada; ≤60 min de review por PR; un work unit por PR con tests/docs incluidos; todo PR encadenado debe declarar start/end/deps/follow-ups/out-of-scope y diagrama de dependencias con `📍`; Feature Branch Chain usa tracker draft/no-merge; diffs contaminados = retarget/rebase; no mezclar estrategias de chain.

### cognitive-doc-design
- Path: `~/.config/opencode/skills/cognitive-doc-design/SKILL.md`
- Trigger: escribir guías, READMEs, RFCs, onboarding, arquitectura, docs review-facing.
- Rules: lead with the answer; progressive disclosure (happy path primero); chunking; signposting; recognition over recall (tablas/checklists); review empathy; estructura default: título outcome-oriented + quick path + details table + checklist + next step.

### comment-writer
- Path: `~/.config/opencode/skills/comment-writer/SKILL.md`
- Trigger: feedback de PR, replies de issues, reviews, mensajes Slack/GitHub.
- Rules: empezar con el punto accionable (no recapitular el PR); warm & direct; 1-3 párrafos cortos; explicar el porqué técnico; no pile-ons (comentar lo de mayor valor); language: español Rioplatense con voseo si el hilo es ES; sin em dashes; fórmula: observación directa → por qué importa → siguiente acción concreta.

### go-testing
- Path: `~/.config/opencode/skills/go-testing/SKILL.md`
- Trigger: tests Go, go test coverage, teatest, golden files.
- Rules: table-driven tests con `t.Run`; testear comportamiento/transiciones de estado, no trivia de implementación; `t.TempDir()` para filesystem; tests integración skippables con `testing.Short()`; golden files deterministas con path `-update`; mocks pequeños en boundaries (sistema/ejecución).

### issue-creation
- Path: `~/.config/opencode/skills/issue-creation/SKILL.md`
- Trigger: crear GitHub issues (bug/feature request).
- Rules: blank issues deshabilitados — usar template (bug_report.yml / feature_request.yml); toda issue nueva recibe `status:needs-review`; maintainer debe poner `status:approved` antes de abrir PR; preguntas van a Discussions, no issues; buscar duplicados antes de crear; bug: campos pre-flight/descripción/repro/expected/actual/OS/agent/shell.

### judgment-day
- Path: `~/.config/opencode/skills/judgment-day/SKILL.md`
- Trigger: `juzgar`, judgment day, dual/adversarial review.
- Rules: resolver project standards del registry ANTES de lanzar judges; DOS judges ciegos en paralelo con IDENTICAL criteria (nunca revisar el código uno mismo); esperar AMBOS antes de síntesis; clasificar warnings real vs teórico; preguntar antes de fixear Round 1; tras fixes, re-lanzar ambos judges antes de commit push done; estados terminales solo APPROVED o ESCALATED; tras 2 iteraciones de fix con issues restantes, preguntar al usuario.

### skill-creator
- Path: `~/.config/opencode/skills/skill-creator/SKILL.md`
- Trigger: nuevos skills, agent instructions, patrones de uso de AI.
- Rules: seguir `docs/skill-style-guide.md` si existe, si no inline fallback; skill = contrato de instrucciones runtime para LLM, no doc humana; sin sección `Keywords` (trigger words en description); references locales; body 180-450 tokens (max 1000); estructura fija: Activation Contract, Hard Rules, Decision Gates, Execution Steps, Output Contract, References; description 1 línea YAML, ≤250 chars, trigger-first.

### work-unit-commits
- Path: `~/.config/opencode/skills/work-unit-commits/SKILL.md`
- Trigger: implementación, splitting de commits, chained PRs, tests/docs con código.
- Rules: commit por work unit (comportamiento verificable), nunca por tipo de archivo; tests en el mismo commit que el behavior; docs con el cambio user-visible; cada commit cuenta una historia y es candidato a chained PR; respetar SDD Review Workload Forecast (Low/Medium/High → single PR / monitorear / seguir delivery_strategy).

## Notes

- Project skills: none found under `.opencode/skills/` (empty).
- Skipped by scan rules: `sdd-*`, `_shared`, `skill-registry`.